# 核料代码

- 整理日期：2026-07-22
- 来源：用户提供的核料相关方法体
- 说明：平台方法签名在界面中配置，本文按调用关系推定方法名并整理；代码逻辑保持原样，未擅自修改。

## 1. 总体调用链

```text
checkInputMaterialForClient
└─ checkInputMaterial
   ├─ enabledCheckInputMaterial
   ├─ synDemandAndSupply
   │  ├─ synInputAndOutputOnStep
   │  │  ├─ synInputAndOutOnStepByOperationBOM
   │  │  └─ synInputAndOutOnStepByBOM
   │  ├─ synMaterialSupply
   │  │  ├─ deleteAllSupplies
   │  │  ├─ synInventoryToMaterialSupply
   │  │  ├─ synIngredientToMaterialSupply
   │  │  ├─ synPurchaseOrderToMaterialSupply
   │  │  ├─ synPurchaseRequestToMaterialSupply
   │  │  ├─ synOtherScenesProductionPlanToMaterialSupply
   │  │  └─ synOutputStepToMaterialSupply
   │  ├─ synMaterialSupplyAvailableQuantity
   │  └─ synMaterialDemand
   │     └─ synInputStepToMaterialDemand
   ├─ clearUnfrozenAndReOccupyFrozenMaterialFulfills
   │  └─ reOccupyFrozenMaterialFulfill
   ├─ assignSuppliesToMaterialDemands
   │  ├─ queryDemandsByReplacePriorityFromInputDemand
   │  └─ assignSuppliesToFirstDemands
   ├─ updateFulfillTimeToPieceStepTime
   │  └─ synAssignResultToPieceSteps
   └─ sendFulfillsToDM
```

## 2. 已提供的代码

### 2.1 客户端入口 `checkInputMaterialForClient`

```java
var materialManager = MaterialManager.getInstance(context)
materialManager.checkInputMaterial()
```

### 2.2 核料主流程 `checkInputMaterial`

```java
var time1 = DateTime.now()
//0.0判断是否开启了核料开关，否则不会走以下逻辑及生成相关实例
if (!this.enabledCheckInputMaterial()) {
    return
}
//1.0同步供应和需求---35s->17
this.synDemandAndSupply()
var time2 = DateTime.now()
Utils.logRunningDuration("同步供应和需求",time1,time2)
//2.0删除现有的未冻结数据，并更新冻结占用---1s
this.clearUnfrozenAndReOccupyFrozenMaterialFulfills()
var time3 = DateTime.now()
Utils.logRunningDuration("删除现有的未冻结数据，并更新冻结占用",time2,time3)
//3.0物料分配---361s->87
var pieceSteps = Group<PieceStep>().toList()
this.assignSuppliesToMaterialDemands()
var time4 = DateTime.now()
Utils.logRunningDuration("物料分配",time3,time4)
//4.0根据分配，计算物料最早可用时间---1392s
this.updateFulfillTimeToPieceStepTime(pieceSteps)
var time5 = DateTime.now()
Utils.logRunningDuration("根据分配，计算物料最早可用时间",time4,time5)
//5.0物料分配发送给DM---15s
this.sendFulfillsToDM()
var time6 = DateTime.now()
Utils.logRunningDuration("0物料分配发送给DM",time5,time6)
```

### 2.3 核料开关 `enabledCheckInputMaterial`

```java
var checkInputMaterial = this.owner.sceneParameter?.checkInputMaterial ?? false
return checkInputMaterial
```

### 2.4 同步供应和需求 `synDemandAndSupply`

```java
if (!this.enabledCheckInputMaterial()) {
    return
}
var time1 = DateTime.now()
this.synInputAndOutputOnStep()
var time2 = DateTime.now()
Utils.logRunningDuration("inputoutput",time1,time2)
this.synMaterialSupply()
var time3 = DateTime.now()
Utils.logRunningDuration("同步供应",time2,time3)
this.synMaterialSupplyAvailableQuantity()
var time4 = DateTime.now()
Utils.logRunningDuration("扣除其他车间占用",time3,time4)
this.synMaterialDemand()
var time5 = DateTime.now()
Utils.logRunningDuration("同步需求",time4,time5)
```

### 2.5 同步步骤投入和产出 `synInputAndOutputOnStep`

```java
var pattern = this.owner.sceneParameter?.orderAndProcessPattern ?? ""
if (pattern == ModelConstants.Pattern23) {
    this.synInputAndOutOnStepByOperationBOM()
} else {
    this.synInputAndOutOnStepByBOM()
}
```

### 2.6 工序BOM模式 `synInputAndOutOnStepByOperationBOM`

```java
if (!this.enabledCheckInputMaterial()) {
    return
}
var data = (List<Map<String, Any>>) Utils.getDataFromDM(context(),"DataSenderManager","sendI23_ImportBomAndOperationToPP",null)
var bomIndex = data.groupBy{ Utils.anyToString(it["queryWorkOrderId"]) }
Group<InputOnStep>().forEach{ it.isDirty = true }
Group<OutputOnStep>().forEach{ it.isDirty = true }
var toAddOutput = new List<OutputOnStep>()
Group<WorkOrder>().forEach{ workOrder ->
    var operationBoms = bomIndex.getOrDefault(workOrder.queryOperationBomId,new List<Map>())
    operationBoms.forEach{ operationBom ->
        this.createInputByOperationBOM(workOrder,operationBoms,operationBom,1.0)
    }
    var lastWorkOrderStep = workOrder.lastWorkOrderStep
    if (lastWorkOrderStep != null) {
        var output = indexing(OutputOnStep.indexByOwnerAndProductId,lastWorkOrderStep,workOrder.productId).firstOrNull()
        if (output == null) {
            output = new OutputOnStep(lastWorkOrderStep)
            toAddOutput.add(output)
        }
        output.isDirty = false
        if (operationBoms.isNotEmpty()) {
            output.updateAttributeByOperationBom(operationBoms.firstOrNull())
        }
    }
}
Group<InputOnStep>().removeIf{ it.isDirty }
Group<OutputOnStep>().removeIf{ it.isDirty }
Group<OutputOnStep>().addAll(toAddOutput)
```

### 2.7 普通BOM模式 `synInputAndOutOnStepByBOM`

```java
if (!this.enabledCheckInputMaterial()) {
    return
}
var semiProductIds = BillOfMaterial.querySemiProductIdsByBom(context())
Group<InputOnStep>().forEach{ it.isDirty = true }
Group<OutputOnStep>().forEach{ it.isDirty = true }
var toAddInput = new List<InputOnStep>()
var toAddOutput = new List<OutputOnStep>()
Group<WorkOrder>().groupBy{ it.productId + it.bomVer }.forEach((productIdAbomver,orders) -> {
    var firstOrder = orders.firstOrNull()
    if (firstOrder == null) {
        return
    }
    var productId = firstOrder.productId
    var ktRows = KT<KTInputConstraint>().select(it.productId == productId,it.stepSequenceNr != "")
    //1.0对有特殊约束的KT，在对应步骤创建input
    ktRows.forEach{ ktRow ->
        var boms = BillOfMaterial.queryByProductIdAndRawMaterialId(context(),productId,ktRow.rawMaterialId).filter{ it.isEffective }
        if (boms.isEmpty()) {
            return
        }
        indexing(WorkOrderStep.indexByProductIdAndSeqNr,productId,ktRow.stepSequenceNr).forEach{ workOrderStep ->
            if (workOrderStep == null) {
                return
            }
            var order = workOrderStep.owner
            if (order == null || !this.canUseBomByInputProductId(order,semiProductIds,ktRow.rawMaterialId)) {
                return
            }
            boms.forEach{ bom ->
                var input = indexing(InputOnStep.indexByOwnerAndMaterialIdAndLineNr,workOrderStep,bom.rawMaterialId,bom.lineNr).firstOrNull()
                if (input == null) {
                    input = new InputOnStep(workOrderStep)
                    toAddInput.add(input)
                }
                input.isDirty = false
                input.updateAttributeByBom(Utils.dataValuesToMap(bom))
            }
        }
    }
    //2.0根据产品代码查找BOM，并对普通BOM在第一步创建input
    var bomsData = BillOfMaterial.queryByProductId(context(),productId).filter{ it.isEffective }
    var hasConstraintRawMaterialIds = ktRows.map{ it.rawMaterialId }
    var commonBoms = bomsData.filter{ !hasConstraintRawMaterialIds.contain(it.rawMaterialId) }
    orders.forEach{ order ->
        indexing(Piece.indexByOwner,order).forEach{ piece ->
            var workOrderStep = piece.firstWorkOrderStep
            if (workOrderStep == null) {
                return
            }
            commonBoms.forEach{ bom ->
                if (!this.canUseBomByInputProductId(order,semiProductIds,bom.rawMaterialId)) {
                    return
                }
                var input = indexing(InputOnStep.indexByOwnerAndMaterialIdAndLineNr,workOrderStep,bom.rawMaterialId,bom.lineNr).firstOrNull()
                if (input == null) {
                    input = toAddInput.find{ it.owner == workOrderStep && it.materialId == bom.rawMaterialId && it.lineNr == bom.lineNr }
                }
                if (input == null) {
                    input = new InputOnStep(workOrderStep)
                    toAddInput.add(input)
                }
                input.isDirty = false
                input.updateAttributeByBom(Utils.dataValuesToMap(bom))
            }
        }
    }
    //3.0对于所有订单的所有末端步骤，创建output
    orders.forEach{ order ->
        var orderProductId = order.productId
        indexing(Piece.indexByOwner,order).forEach{ piece ->
            var workOrderStep = piece.selectedWorkOrderSteps.sortedBy{ it.sequenceNr }.lastOrNull()
            if (workOrderStep == null) {
                return
            }
            var output = indexing(OutputOnStep.indexByOwnerAndProductId,workOrderStep,orderProductId).firstOrNull()
            if (output == null) {
                output = toAddOutput.find{ it.owner == workOrderStep && Utils.anyToString(it.productId) == Utils.anyToString(orderProductId) }
            }
            if (output == null) {
                output = new OutputOnStep(workOrderStep)
                toAddOutput.add(output)
            }
            output.isDirty = false
            if (bomsData.isNotEmpty()) {
                output.updateAttributeByBom(Utils.dataValuesToMap(bomsData.firstOrNull()))
            }
        }
    }
})
Group<InputOnStep>().removeIf{ it.isDirty }
Group<OutputOnStep>().removeIf{ it.isDirty }
Group<InputOnStep>().addAll(toAddInput)
Group<OutputOnStep>().addAll(toAddOutput)
```

### 2.8 同步全部供应 `synMaterialSupply`

```java
if (!this.enabledCheckInputMaterial()) {
    return
}
this.deleteAllSupplies()
this.synInventoryToMaterialSupply()
this.synIngredientToMaterialSupply()
this.synPurchaseOrderToMaterialSupply()
this.synPurchaseRequestToMaterialSupply()
this.synOtherScenesProductionPlanToMaterialSupply()
this.synOutputStepToMaterialSupply()
```

### 2.9 删除全部供应 `deleteAllSupplies`

```java
var dh = context().getDbHelper()
QMaterialSupply qSupply = QMaterialSupply.qmaterialsupply
var supplyData = dh.dslSelectFrom(qSupply).findAll()
dh.deleteBatch(supplyData)
```

### 2.10 库存转供应 `synInventoryToMaterialSupply`

```java
var inventories = InventoryIn.queryAllData(context())
var toAdd = new List<MaterialSupply>()
var dh = context().getDbHelper()
var semiProductIds = BillOfMaterial.querySemiProductIdsByBom(context())
inventories.groupBy{ it.productId }.forEach((productId,inventoryList) -> {
    var product = indexing(Product.indexByProductId,productId).firstOrNull()
    var detectDuration = product?.detectDuration ?? Duration.ZERO
    var canSelfProduction = product?.canSelfProduction ?? false
    var isBanChengPing = semiProductIds.contain(productId)
    inventoryList.forEach{ inventory ->
        if (isBanChengPing) {
            var semiQty = inventory.quantity
            if (semiQty > Constant.EPS) {
                var obj = new MaterialSupply()
                MaterialSupply.updateAttributeBySemiInventoryNode(obj,inventory)
                obj.detectDuration = detectDuration
                obj.canSelfProduction = canSelfProduction
                this.setSemiFinishedInventoryDate(obj,inventory)
                this.setSemiProductVirtualQuantity(obj,productId)
                MaterialSupply.updateSupplySortKey(obj)
                if (obj.supplyQuantity > Constant.EPS) {
                    toAdd.add(obj)
                }
            }
            return
        }
        var availableQty = inventory.quantity
        if (availableQty > Constant.EPS) {
            var obj = new MaterialSupply()
            MaterialSupply.updateAttributeByInventoryAvailableNode(obj,inventory)
            obj.detectDuration = detectDuration
            obj.canSelfProduction = canSelfProduction
            obj.availableReleaseTime = obj.availableTime
            obj.availableReleaseDate = obj.availableDate
            MaterialSupply.updateSupplySortKey(obj)
            if (obj.supplyQuantity > Constant.EPS) {
                toAdd.add(obj)
            }
        }
        var detectQty = inventory.detectQuantity
        if (detectQty > Constant.EPS) {
            var obj = new MaterialSupply()
            MaterialSupply.updateAttributeByInventoryDetectNode(obj,inventory)
            obj.detectDuration = detectDuration
            obj.canSelfProduction = canSelfProduction
            obj.availableReleaseTime = TimeCalc.timePlusDuration(obj.availableTime,detectDuration)
            obj.availableReleaseDate = obj.availableReleaseTime.toDate()
            MaterialSupply.updateSupplySortKey(obj)
            if (obj.supplyQuantity > Constant.EPS) {
                toAdd.add(obj)
            }
        }
    }
})
dh.insertBatch(toAdd)
```

### 2.11 分批到货原料药转供应 `synIngredientToMaterialSupply`

```java
var data = (List<Map<String, Any>>) Utils.getDataFromDM(context(),"DataSenderManager","sendI36_ImportSupplyPlanToPP",null)
var toAdd = new List<MaterialSupply>()
var dh = context().getDbHelper()
var count = 1
data.groupBy{ Utils.anyToString(it["materialId"]) }.forEach((materialId,inventories) -> {
    inventories.forEach{ inventory ->
        var obj = new MaterialSupply()
        MaterialSupply.updateAttributeByIngredientFromDM(obj,inventory)
        if (obj.nr == "") {
            obj.nr = "yuanliaoyao" + "-" + count.toString()
        }
        if (obj.onlyNr == "") {
            obj.onlyNr = "yuanliaoyao" + "-" + count.toString()
        }
        MaterialSupply.updateSupplySortKey(obj)
        toAdd.add(obj)
        count += 1
    }
})
var c = 1
dh.insertBatch(toAdd)
```

### 2.12 采购订单转供应 `synPurchaseOrderToMaterialSupply`

```java
var toAdd = new List<MaterialSupply>()
var purchaseOrders = PurchaseOrderln.queryAllData(context())
purchaseOrders.forEach{ po ->
    if (po.quantity <= Constant.EPS) {
        return
    }
    var obj = new MaterialSupply()
    MaterialSupply.updateAttributeByPurchaseOrderln(obj,po)
    var product = indexing(Product.indexByProductId,po.productId).firstOrNull()
    var detectDuration = product?.detectDuration ?? Duration.ZERO
    obj.detectDuration = detectDuration
    obj.canSelfProduction = product?.canSelfProduction ?? false
    obj.availableReleaseTime = TimeCalc.timePlusDuration(obj.availableTime,detectDuration)
    obj.availableReleaseDate = obj.availableReleaseTime.toDate()
    MaterialSupply.updateSupplySortKey(obj)
    if (obj.supplyQuantity > Constant.EPS) {
        toAdd.add(obj)
    }
}
context().getDbHelper().insertBatch(toAdd)
```

### 2.13 其他场景生产计划转供应 `synOtherScenesProductionPlanToMaterialSupply`

```java
var data = (List<Map<String, Any>>) Utils.getDataFromDM(context(),"DataSenderManager","sendPP_ProductionPlanToPP",null)
var toAdd = new List<MaterialSupply>()
var dh = context().getDbHelper()
data.groupBy{ Utils.anyToString(it["productId"]) }.forEach((productId,productions) -> {
    var product = indexing(Product.indexByProductId,productId).firstOrNull()
    productions.forEach{ production ->
        var obj = new MaterialSupply()
        MaterialSupply.updateAttributeByProductionPlanFromDM(obj,production,product)
        MaterialSupply.updateSupplySortKey(obj)
        toAdd.add(obj)
    }
})
dh.insertBatch(toAdd)
```

### 2.14 本车间产出转供应 `synOutputStepToMaterialSupply`

```java
var toAdd = new List<MaterialSupply>()
var dh = context().getDbHelper()
var materialSupply = QMaterialSupply.qmaterialsupply
var oldmaterialSupply = dh.dslSelectFrom(materialSupply).where(materialSupply.workShopId.eq(context().sceneCode())).findAll()
dh.deleteBatch(oldmaterialSupply)
Group<PieceStep>().forEach{ pieceStep ->
    pieceStep.workOrderStep?.outputsOnStep?.forEach{ output ->
        var obj = new MaterialSupply()
        MaterialSupply.updateAttributeByPieceStepAndOutput(context(),obj,pieceStep,output)
        MaterialSupply.updateSupplySortKey(obj)
        toAdd.add(obj)
    }
}
dh.insertBatch(toAdd)
```

### 2.15 扣除其他车间占用 `synMaterialSupplyAvailableQuantity`

```java
if (!this.enabledCheckInputMaterial()) {
    return
}
var time1 = DateTime.now()
var data = (List<Map<String, Any>>) Utils.getDataFromDM(context(),"DataSenderManager","sendIPP_MaterialFulfillToPP",null)
var dh = context().getDbHelper()
var time2 = DateTime.now()
Utils.logRunningDuration("读取",time1,time2)
data.removeIf{ Utils.anyToString(it["ppProjectId"]) == context().projectCode() && Utils.anyToString(it["ppSceneId"]) == context().sceneCode() }
var time3 = DateTime.now()
Utils.logRunningDuration("移除",time2,time3)
var fulfillMap = data.groupBy{ Utils.anyToString(it["supplyUniqueId"]) }
var qSupply = QMaterialSupply.qmaterialsupply
var thisSceneId = context().sceneCode()
var supplies = dh.dslSelectFrom(qSupply).findAll()
var toUpdate = new List<MaterialSupply>()
supplies.forEach{ supply ->
    var fulfills = fulfillMap.getOrDefault(supply.uniqueId,new List<Map>())
    if (fulfills.isNotEmpty()) {
        toUpdate.add(supply)
        supply.occupiedByOtherWorkshop = fulfills.sumByDouble{ Utils.anyToDouble(it["quantity"]) }
        supply.availableQuantity = maxOf(0.0,supply.supplyQuantity - supply.occupiedByOtherWorkshop)
        supply.leftQuantity = supply.availableQuantity.roundHalfDown(7)
    }
}
var time4 = DateTime.now()
Utils.logRunningDuration("更新",time3,time4)
dh.updateBatch(supplies)
```

### 2.16 同步需求 `synMaterialDemand`

```java
if (!this.enabledCheckInputMaterial()) {
    return
}
this.synInputStepToMaterialDemand()
```

### 2.17 根据步骤投入生成需求 `synInputStepToMaterialDemand`

```java
var toAdd = new List<MaterialDemand>()
var time1 = DateTime.now()
var dh = context().getDbHelper()
var qdemand = QMaterialDemand.qmaterialdemand
dh.dslDelete(qdemand).execute()
var time2 = DateTime.now()
Utils.logRunningDuration("删除",time1,time2)
Group<PieceStep>().forEach{ pieceStep ->
    if (pieceStep.cannotCheckInputReason != "") {
        return
    }
    pieceStep.workOrderStep?.inputsOnStep?.forEach{ input ->
        var obj = new MaterialDemand()
        MaterialDemand.updateAttributeByPieceStepAndInput(obj,pieceStep,input)
        toAdd.add(obj)
    }
}
dh.insertBatch(toAdd)
var time3 = DateTime.now()
Utils.logRunningDuration("插入" + toAdd.size(),time2,time3)
```

### 2.18 清理未冻结分配并重占冻结分配 `clearUnfrozenAndReOccupyFrozenMaterialFulfills`

```java
if (!this.enabledCheckInputMaterial()) {
    return
}
var time1 = DateTime.now()
var toAdd = new List<MaterialDemand>()
var dh = context().getDbHelper()
var qfulfill = QMaterialFulfill.qmaterialfulfill
var oldFulfills = dh.dslSelectFrom(qfulfill).where(qfulfill.frozenFlag.eq(false) || qfulfill.frozenFlag.isNull()).findAll()
var time2 = DateTime.now()
Utils.logRunningDuration("查找未冻结分配",time1,time2)
dh.deleteBatch(oldFulfills)
var time3 = DateTime.now()
Utils.logRunningDuration("删除未冻结分配",time2,time3)
this.reOccupyFrozenMaterialFulfill()
var time4 = DateTime.now()
Utils.logRunningDuration("更新信息给需求和供应",time3,time4)
```

### 2.19 冻结分配重占 `reOccupyFrozenMaterialFulfill`

```java
var dh = context().getDbHelper()
var needUpdateDemands = new List<MaterialDemand>()
var needUpdateSupplies = new List<MaterialSupply>()
var invalidFrozenFulfills = new List<MaterialFulfill>()
var qfulfill = QMaterialFulfill.qmaterialfulfill
var frozenFulfills = dh.dslSelectFrom(qfulfill).where(qfulfill.frozenFlag.eq(true)).findAll()
var supplies = MaterialSupply.queryAllData(context())
var supplyMap = supplies.groupBy{ it.uniqueId }
frozenFulfills.groupBy{ it.demandUniqueId }.forEach((demandUniqueId,fulfills) -> {
    var demand = MaterialDemand.findByUniqueId(context(),demandUniqueId)
    if (demand == null) {
        invalidFrozenFulfills.addAll(fulfills)
        return
    }
    var firstPriorityDemand = MaterialDemand.findFirstReplacePriorityByMaterialDemand(context(),demand)
    var noLocationIds = MaterialSupply.queryNoLocationIdsByProductId(context(),demand.productId,demand.pieceSizeQuantity,demand.routingId)
    var isGuojiBanchengpingDemand = MaterialSupply.isGuojiBanchengpingMaterialDemand(demand,context())
    var handBooks = new List<KTProcessingTradeHandBook>()
    var candidateMaterialIds = new List<String>()
    if (isGuojiBanchengpingDemand) {
        handBooks = MaterialSupply.queryProcessingTradeHandBooks(context(),demand.productId,demand.materialId)
        candidateMaterialIds = handBooks.map{ it.materialId ?? "" }.filter{ it != "" }
        var demandMaterialId = demand.materialId ?? ""
        if (!candidateMaterialIds.contain(demandMaterialId)) {
            candidateMaterialIds.add(demandMaterialId)
        }
    }
    var hasValidFulfill = false
    fulfills.forEach{ fulfill ->
        var supply = supplyMap.getOrDefault(fulfill.supplyUniqueId,new List<MaterialSupply>()).firstOrNull()
        if (supply == null) {
            invalidFrozenFulfills.add(fulfill)
            return
        }
        //24039：校验冻结库存供应是否命中禁用库位
        var locationId = supply.sapStockingPointId ?? ""
        var invalidLocation = MaterialSupply.isInventorySupply(supply) && locationId != "" && noLocationIds.contain(locationId)
        if (invalidLocation) {
            invalidFrozenFulfills.add(fulfill)
            return
        }
        //24040：国际订单半成品校验手册物料和库位
        if (isGuojiBanchengpingDemand && !handBooks.isEmpty() && MaterialSupply.isInventorySupply(supply)) {
            var handBookSupplies = supplies.filter{ candidateMaterialIds.contain(it.productId ?? "") }
            var legalHandBookSupplies = new List<MaterialSupply>()
            handBookSupplies.forEach{ handBookSupply ->
                var handBookLocationId = handBookSupply.sapStockingPointId ?? ""
                var handBookInvalidLocation = MaterialSupply.isInventorySupply(handBookSupply) && handBookLocationId != "" && noLocationIds.contain(handBookLocationId)
                if (!handBookInvalidLocation) {
                    legalHandBookSupplies.add(handBookSupply)
                }
            }
            MaterialSupply.filterProcessingTradeHandBookSuppliesInPlace(legalHandBookSupplies,handBooks)
            if (!legalHandBookSupplies.contain(supply)) {
                invalidFrozenFulfills.add(fulfill)
                return
            }
        }
        demand.fulfillQuantity += fulfill.quantity
        hasValidFulfill = true
        if (firstPriorityDemand != null) {
            var demandQty = demand.pieceStepQuantity * demand.inputFactor
            var ratio = demandQty > 0.0 ? fulfill.quantity / demandQty : 0.0
            firstPriorityDemand.leftRatio -= ratio
        }
        supply.leftQuantity -= fulfill.quantity
        supply.leftQuantity.roundHalfDown(7)
        if (!needUpdateSupplies.contain(supply)) {
            needUpdateSupplies.add(supply)
        }
        fulfill.availableDate = supply.availableReleaseDate
        fulfill.availableTime = supply.availableReleaseTime
    }
    if (hasValidFulfill) {
        if (!needUpdateDemands.contain(demand)) {
            needUpdateDemands.add(demand)
        }
        if (firstPriorityDemand != null && firstPriorityDemand != demand && !needUpdateDemands.contain(firstPriorityDemand)) {
            needUpdateDemands.add(firstPriorityDemand)
        }
    }
})
if (!invalidFrozenFulfills.isEmpty()) {
    dh.deleteBatch(invalidFrozenFulfills)
}
var validFrozenFulfills = frozenFulfills.filter{ !invalidFrozenFulfills.contain(it) }
if (!needUpdateDemands.isEmpty()) {
    dh.updateBatch(needUpdateDemands)
}
if (!needUpdateSupplies.isEmpty()) {
    dh.updateBatch(needUpdateSupplies)
}
if (!validFrozenFulfills.isEmpty()) {
    dh.updateBatch(validFrozenFulfills)
}
```

### 2.20 正式分配入口 `assignSuppliesToMaterialDemands`

```java
if (!this.enabledCheckInputMaterial()) {
    return
}
var dh = context().getDbHelper()
var supplyNode = MaterialSupply.queryAllData(context())
var demandNode = MaterialDemand.queryAllData(context())
var firstDemands = MaterialDemand.queryDemandsByReplacePriorityFromInputDemand(demandNode)
var toAddFulfills = MaterialDemand.assignSuppliesToFirstDemands(context(),firstDemands,supplyNode,demandNode)
dh.updateBatch(supplyNode)
dh.updateBatch(demandNode)
dh.insertBatch(toAddFulfills)
```

### 2.21 需求按替代优先级整理 `queryDemandsByReplacePriorityFromInputDemand`

```java
var outputDemand = new List<MaterialDemand>()
if (inputDemand == null || inputDemand.isEmpty()) {
    return outputDemand
}
var productDemandMap = inputDemand.groupBy{ demand -> demand.productId }
productDemandMap.values().forEach{ productDemands ->
    var replaceDemands = productDemands.filter{ !it.replaceGroupId.isEmpty() }
    var normalDemands = productDemands.filter{ it.replaceGroupId.isEmpty() }
    var handledReplaceGroupKeys = new List<String>()
    replaceDemands.forEach{ demand ->
        var pieceId = Utils.anyToString(demand.pieceId)
        var replaceGroupId = Utils.anyToString(demand.replaceGroupId)
        var replaceGroupKey = pieceId + "|" + replaceGroupId
        if (!handledReplaceGroupKeys.contain(replaceGroupKey)) {
            var currentReplaceGroupDemands = replaceDemands.filter{ currentDemand ->
                var currentPieceId = Utils.anyToString(currentDemand.pieceId)
                var currentReplaceGroupId = Utils.anyToString(currentDemand.replaceGroupId)
                var currentReplaceGroupKey = currentPieceId + "|" + currentReplaceGroupId
                return currentReplaceGroupKey == replaceGroupKey
            }
            currentReplaceGroupDemands = currentReplaceGroupDemands.sortedBy{ currentDemand ->
                var replacePriorityStr = Utils.anyToString(currentDemand.replacePriority)
                return replacePriorityStr.isEmpty() ? Int.MAX : Utils.anyToInt(replacePriorityStr)
            }
            outputDemand.addAll(currentReplaceGroupDemands)
            handledReplaceGroupKeys.add(replaceGroupKey)
        }
    }
    outputDemand.addAll(normalDemands)
}
return outputDemand
```

### 2.22 按需求执行分配 `assignSuppliesToFirstDemands`

```java
var totalToAddFulfill = new List<MaterialFulfill>()
var canMixConsumeSet = SceneParameter.getInstance(context)?.canMixConsumeSet ?? false
var newMaterialDemands = new List<MaterialDemand>()
var hasConsumeSetMap = materialDemands.groupBy{ it.consumeSet != "" }
var hasConsumeDemands = hasConsumeSetMap.getOrDefault(true,new List<MaterialDemand>()).distinctBy{ it.pieceStepId + it.consumeSet }
var hasNotConsumeDemands = hasConsumeSetMap.getOrDefault(false,new List<MaterialDemand>())
newMaterialDemands.addAll(hasConsumeDemands)
newMaterialDemands.addAll(hasNotConsumeDemands)
var sortedFirstDemands = MaterialDemand.sortedByDueDateAndPieceStepId(newMaterialDemands)
var replaceDemandMap = sortedFirstDemands.filter{ it.consumeSet == "" && !it.replaceGroupId.isEmpty() }.groupBy{ it.pieceId + it.replaceGroupId }
var consumeMap = materialDemands.groupBy{ it.pieceStepId + it.consumeSet }
var handledReplaceGroupKeys = new List<String>()
sortedFirstDemands.forEach{ firstDemand ->
    if (firstDemand.pieceId == "A001-10") {
        var c = 1
    }
    if (firstDemand.consumeSet == "") {
        var replaceGroupId = firstDemand.replaceGroupId
        if (replaceGroupId.isEmpty()) {
            var tuple = MaterialDemand.assignSuppliesToFirstReplaceDemand(context,firstDemand,supplyNode,demandNode,false,false,0.0)
            totalToAddFulfill.addAll(tuple[1])
        } else {
            var replaceGroupKey = firstDemand.pieceId + replaceGroupId
            if (!handledReplaceGroupKeys.contain(replaceGroupKey)) {
                var replaceDemandGroup = replaceDemandMap.getOrDefault(replaceGroupKey,new List<MaterialDemand>())
                replaceDemandGroup = replaceDemandGroup.sortedBy{ it.replacePriority }
                var tuple = MaterialDemand.assignSuppliesToFirstReplaceDemand1(context,replaceDemandGroup,supplyNode,false,false,0.0)
                totalToAddFulfill.addAll(tuple[1])
                handledReplaceGroupKeys.add(replaceGroupKey)
            }
        }
    } else {
        var demandSet = consumeMap.getOrDefault(firstDemand.pieceStepId + firstDemand.consumeSet,new List<MaterialDemand>())
        var tuple = MaterialDemand.assignSuppliesToFirstDemandSet(context,demandSet,supplyNode,demandNode,canMixConsumeSet)
        totalToAddFulfill.addAll(tuple[1])
    }
}
return totalToAddFulfill
```

### 2.23 根据分配更新步骤 `updateFulfillTimeToPieceStepTime`

```java
if (!this.enabledCheckInputMaterial()) {
    return
}
this.synAssignResultToPieceSteps(pieceSteps)
```

### 2.24 将分配结果回写需求和步骤 `synAssignResultToPieceSteps`

```java
var time1 = DateTime.now()
var demandNode = MaterialDemand.queryAllData(context())
var fulfillNode = MaterialFulfill.queryAllData(context())
var demandMap = demandNode.groupBy{ it.pieceStepId }
var fulfillMap = fulfillNode.groupBy{ it.demandPieceStepId }
var needUpdateDemands = new List<MaterialDemand>()
pieceSteps.forEach{ pieceStep ->
    var demands = demandMap.getOrDefault(pieceStep.pieceStepId,new List<MaterialDemand>())
    needUpdateDemands.addAll(demands)
    var fulfills = fulfillMap.getOrDefault(pieceStep.pieceStepId,new List<MaterialFulfill>())
    MaterialDemand.updateAssignResultToDemandAndPS(context(),pieceStep,fulfills,demands)
}
var time2 = DateTime.now()
Utils.logRunningDuration("完整更新需求",time1,time2)
this.updatePieceStepDownStream()
var time3 = DateTime.now()
Utils.logRunningDuration("上下游更新",time2,time3)
var dh = context().getDbHelper()
dh.updateBatch(needUpdateDemands)
var time4 = DateTime.now()
Utils.logRunningDuration("更新需求给DB",time3,time4)
```

### 2.25 公共供应过滤 `MaterialSupply.queryAvailableFromInputSupplies`

```java
var supplyMap = inputSupplies.groupBy{ it.productId }
var isGuojiChengpingUseBomBanchengping = MaterialSupply.isGuojiChengpingUseBomBanchengping(materialDemand,conetxt)
var isGuojiBanchengpingDemand = MaterialSupply.isGuojiBanchengpingMaterialDemand(materialDemand,conetxt)
var handBooks = new List<KTProcessingTradeHandBook>()
var outputSupplies = new List<MaterialSupply>()
if (isGuojiBanchengpingDemand) {
    handBooks = MaterialSupply.queryProcessingTradeHandBooks(conetxt,materialDemand.productId,materialDemand.materialId)
    var candidateMaterialIds = handBooks.map{ it.materialId ?? "" }.filter{ it != "" }
    var demandMaterialId = materialDemand.materialId ?? ""
    if (!candidateMaterialIds.contain(demandMaterialId)) {
        candidateMaterialIds.add(demandMaterialId)
    }
    outputSupplies = inputSupplies.filter{ candidateMaterialIds.contain(it.productId ?? "") }
} else {
    outputSupplies = supplyMap.getOrDefault(materialDemand.materialId,new List<MaterialSupply>())
}
//24039：按当前生产产品过滤禁用库存库位
var noLocationIds = MaterialSupply.queryNoLocationIdsByProductId(conetxt,materialDemand.productId,materialDemand.pieceSizeQuantity,materialDemand.routingId)
var availableSupplies = new List<MaterialSupply>()
outputSupplies.forEach{ supply ->
    var locationId = supply.sapStockingPointId ?? ""
    var isForbidden = MaterialSupply.isInventorySupply(supply) && locationId != "" && noLocationIds.contain(locationId)
    if (!isForbidden) {
        availableSupplies.add(supply)
    }
}
outputSupplies = availableSupplies
//半成品按照设备组禁用物料过滤
var productDemand = conetxt.KT<KTSingleResourceSpeedExtend>().filter{ it.productId == materialDemand.productId }
if (!productDemand.isEmpty()) {
    var scheduledPieceSteps = conetxt.Group<PieceStep>().filter{
        it.productId == materialDemand.productId &&
        it.pieceId == materialDemand.pieceId &&
        it.isScheduled == true
    }
    if (!scheduledPieceSteps.isEmpty()) {
        scheduledPieceSteps.forEach{ p ->
            var usedResourceGroupId = conetxt.Group<Resource>().filter{ it.resourceId == p.usedResourceId }.firstOrNull()?.resourceGroupId ?? ""
            if (usedResourceGroupId != "") {
                var materialsID = productDemand.filter{ it.resourceGroupId == usedResourceGroupId }.firstOrNull()?.NoMaterialsID ?? ""
                if (materialsID != "") {
                    outputSupplies.removeIf{ it.productId == materialsID }
                }
            }
        }
    }
}
var banchengpingIds = conetxt.KT<KTProductRouting>().filter{
    (it?.productType1 ?? "") == "B" &&
    it.customType == "国内订单"
}.map{ it.productId }
var isDomesticSemiDemand = banchengpingIds.contain(materialDemand.materialId)
//数量过滤
if (isGuojiChengpingUseBomBanchengping) {
    outputSupplies.removeIf{ MaterialSupply.getGuojiBanchengpingAvailableQty(it) <= Constant.EPS }
} else if (isDomesticSemiDemand) {
    outputSupplies.removeIf{ it.leftQuantityVritual <= Constant.EPS }
} else {
    outputSupplies.removeIf{ it.leftQuantity < leastQty }
}
//国际成品BOM半成品按生产月份过滤
if (isGuojiChengpingUseBomBanchengping) {
    MaterialSupply.filterGuojiBanchengpingByProductionMonthInPlace(outputSupplies,materialDemand,conetxt)
}
//不可混批
if (materialDemand.forbiddenMix && !isGuojiChengpingUseBomBanchengping) {
    if (isDomesticSemiDemand) {
        outputSupplies.removeIf{ it.leftQuantityVritual <= Constant.EPS }
    } else {
        outputSupplies.removeIf{ it.leftQuantity < materialDemand.demandQuantity }
    }
}
//有效期
if (materialDemand.expireConstraint) {
    outputSupplies.removeIf{ MaterialSupply.isInventorySupply(it) && it.expireDate < materialDemand.dueDate }
}
//指定供应商
if (materialDemand.supplierConstraint) {
    var supplierIds = Utils.splitStringToStringList(materialDemand.supplierIds)
    outputSupplies.removeIf{ !supplierIds.contain(it.supplierId) }
}
//指定上游车间
if (materialDemand.upStreamWorkshopConstraint) {
    var workshopIds = Utils.splitStringToStringList(materialDemand.upStreamWorkshopIds)
    outputSupplies.removeIf{ !workshopIds.contain(it.workShopId) }
}
//24040：国际订单半成品按手册号过滤物料和库存库位
if (isGuojiBanchengpingDemand) {
    MaterialSupply.filterProcessingTradeHandBookSuppliesInPlace(outputSupplies,handBooks)
}
return outputSupplies
```

### 2.26 普通需求和顺位替代料分配 `assignSuppliesToFirstReplaceDemand`

```java
var totalFulfillRatio = 0.0
var totalToAddFulfill = new List<MaterialFulfill>()
var supplies = MaterialSupply.queryAvailableFromInputSupplies(supplyNode,firstDemand,Constant.EPS,context)
var totalRemainRatio = hasUserFulfillRatio ? userFulfillRatio : firstDemand.leftRatio
var fulfillTuple = MaterialDemand.assignAvailableSuppliesToDemand(context,supplies,firstDemand,firstDemand,false,tempFlag,hasUserFulfillRatio,totalRemainRatio)
var fulfillRatio = fulfillTuple[0]
var toAddFulfill = fulfillTuple[1]
totalRemainRatio -= fulfillRatio
totalFulfillRatio += fulfillRatio
totalToAddFulfill.addAll(toAddFulfill)
if (totalRemainRatio <= Constant.EPS) {
    return (totalFulfillRatio,totalToAddFulfill)
}
if (firstDemand.replaceGroupId == "") {
    return (totalFulfillRatio,totalToAddFulfill)
}
var demandGroup = MaterialDemand.queryNextReplacePriorityInSameSetFromInput(context,demandNode,firstDemand.pieceId,firstDemand.materialId,firstDemand.replaceGroupId,firstDemand.consumeSet).sortedBy{ it.replacePriority }
while (totalRemainRatio > Constant.EPS && demandGroup.isNotEmpty()) {
    var demand = demandGroup.firstOrNull()
    supplies = MaterialSupply.queryAvailableFromInputSupplies(supplyNode,demand,firstDemand.demandQuantity,context)
    fulfillTuple = MaterialDemand.assignAvailableSuppliesToDemand(context,supplies,firstDemand,demand,false,tempFlag,hasUserFulfillRatio,totalRemainRatio)
    fulfillRatio = fulfillTuple[0]
    toAddFulfill = fulfillTuple[1]
    totalRemainRatio -= fulfillRatio
    totalFulfillRatio += fulfillRatio
    totalToAddFulfill.addAll(toAddFulfill)
    demandGroup.remove(demand)
}
return (totalFulfillRatio,totalToAddFulfill)
```

### 2.27 整组替代需求分配 `assignSuppliesToFirstReplaceDemand1`

```java
var totalFulfillRatio = 0.0
var totalToAddFulfill = new List<MaterialFulfill>()
var allSupplies = new List<MaterialSupply>()
demands.forEach{ demand ->
    var supplies = MaterialSupply.queryAvailableFromInputSupplies(supplyNode,demand,Constant.EPS,context)
    allSupplies.addAll(supplies)
}
var totalRemainRatios = new List<Double>()
demands.forEach{ demand ->
    var totalRemainRatio = hasUserFulfillRatio ? userFulfillRatio : demand.leftRatio
}
var fulfillTuple = MaterialDemand.assignAvailableSuppliesToDemand1(context,allSupplies,demands,false,tempFlag,hasUserFulfillRatio)
var fulfillRatio = fulfillTuple[0]
var toAddFulfill = fulfillTuple[1]
totalToAddFulfill.addAll(toAddFulfill)
return (fulfillRatio,totalToAddFulfill)
```

### 2.28 配套组及替代配套组分配 `assignSuppliesToFirstDemandSet`

```java
var totalAssignRatio = 0.0
var totalToAddFulfill = new List<MaterialFulfill>()
var leftRatio = firstDemandSet.firstOrNull()?.leftRatio ?? 0.0
var assignTuple = MaterialDemand.assignSuppliesToDemandSet(context,firstDemandSet,firstDemandSet,supplyNode,demandNode,canMixSet)
var assignRatio = assignTuple[0]
var assignFulfill = assignTuple[1]
totalAssignRatio += assignRatio
totalToAddFulfill.addAll(assignFulfill)
leftRatio -= assignRatio
if (leftRatio > Constant.EPS) {
    var firstConsumeSetPriority = firstDemandSet.firstOrNull()?.consumeSetPriority ?? 0.0
    var pieceId = firstDemandSet.firstOrNull()?.pieceId ?? ""
    var nextPrioritySet = new List<MaterialDemand>()
    var tempList = new List<MaterialDemand>()
    firstDemandSet.forEach{ demand ->
        var toAdd = MaterialDemand.queryNextSetPriorityBySameReplaceFromInput(context,demandNode,pieceId,demand.replaceGroupId,firstConsumeSetPriority)
        tempList.addAll(toAdd)
    }
    var consumeSets = tempList.map{ it.consumeSet }.distinct()
    consumeSets.forEach{ consumeSet ->
        var demandSet = MaterialDemand.queryByConsumeSetFromInput(context,demandNode,pieceId,consumeSet)
        nextPrioritySet.addAll(demandSet)
    }
    var consumeMap = nextPrioritySet.groupBy{ it.consumeSet }
    nextPrioritySet.distinctBy{ it.consumeSet }.sortedBy{ it.consumeSetPriority }.forEach{ leadDemand ->
        var demandSet = consumeMap.getOrDefault(leadDemand.consumeSet,new List<MaterialDemand>())
        assignTuple = MaterialDemand.assignSuppliesToDemandSet(context,firstDemandSet,demandSet,supplyNode,demandNode,canMixSet)
        assignRatio = assignTuple[0]
        assignFulfill = assignTuple[1]
        totalAssignRatio += assignRatio
        totalToAddFulfill.addAll(assignFulfill)
        leftRatio -= assignRatio
    }
}
return (totalAssignRatio,totalToAddFulfill)
```

### 2.29 回写需求和批次步骤结果 `MaterialDemand.updateAssignResultToDemandAndPS`

```java
var fulfillMap = fulfills.groupBy{ it.demandUniqueId }
var upstreamPieceSteps = new RSet<PieceStep>()
demands.forEach{ demand ->
    var fulfills2 = fulfillMap.getOrDefault(demand.uniqueId,new List<MaterialFulfill>())
    if (demand.leftRatio < Constant.EPS) {
        demand.materialAvailableTime = fulfills2.maxByOrNull{ it.availableTime }?.availableTime ?? Constant.DATETIME_MIN
        demand.materialAvailableTimeByOutside = fulfills2.filter{ it.supplyDataSource != ModelConstants.FromThisSceneProductionPlan }.maxByOrNull{ it.availableTime }?.availableTime ?? Constant.DATETIME_MIN
        demand.materialAvailableTimeByUpStreamManual = fulfills2.filter{ it.supplyDataSource == ModelConstants.FromThisSceneProductionPlan }.maxByOrNull{ it.availableTime }?.availableTime ?? Constant.DATETIME_MIN
    } else {
        var material = context.indexing(Product.indexByProductId,demand.materialId).firstOrNull()
        demand.materialAvailableTime = material?.earliestAvailableTime ?? Constant.DATETIME_MIN
        demand.materialAvailableTimeByOutside = material?.earliestAvailableTime ?? Constant.DATETIME_MIN
        demand.materialAvailableTimeByUpStreamManual = fulfills2.filter{ it.supplyDataSource == ModelConstants.FromThisSceneProductionPlan }.maxByOrNull{ it.availableTime }?.availableTime ?? Constant.DATETIME_MIN
    }
    demand.inventoryFulfillQuantity = fulfills2.filter{ it.supplyDataSource == ModelConstants.FromInventory }.sumByDouble{ it.quantity }
    demand.productCompleteQuantity = demand.inputFactor > 0 ? demand.fulfillQuantity / demand.inputFactor : 0.0
    demand.productCompleteQuantityByInv = demand.inputFactor > 0 ? demand.inputFactor / demand.inputFactor : 0.0
    fulfills2.forEach{ fulfill ->
        if (fulfill.supplyDataSource == ModelConstants.FromThisSceneProductionPlan) {
            var ps = context.indexing(PieceStep.indexByPieceStepId,fulfill.supplyPieceStepId).firstOrNull()
            if (ps != null) {
                upstreamPieceSteps.add(ps)
            }
        }
    }
}
pieceStep.materialAvailableTimeByOutside = demands.filter{ it.isWhole }.maxByOrNull{ it.materialAvailableTimeByOutside }?.materialAvailableTimeByOutside ?? Constant.DATETIME_MIN
pieceStep.materialAvailableTimeByUpStreamManual = demands.filter{ it.isWhole }.maxByOrNull{ it.materialAvailableTimeByUpStreamManual }?.materialAvailableTimeByUpStreamManual ?? Constant.DATETIME_MIN
pieceStep.totalCompleteQuantity = demands.minByOrNull{ it.productCompleteQuantity }?.productCompleteQuantity ?? 0.0
pieceStep.inventoryCompleteQuantity = demands.minByOrNull{ it.productCompleteQuantityByInv }?.productCompleteQuantityByInv ?? 0.0
pieceStep.upStreamPieceSteps = upstreamPieceSteps
```

### 2.30 发送核料结果给DM `sendFulfillsToDM`

```java
if (!this.enabledCheckInputMaterial()) {
    return
}
var dh = context().getDbHelper()
var qFulfill = QMaterialFulfill.qmaterialfulfill
var fulfills = dh.dslSelectFrom(qFulfill).findAll()
var args = new Map<String, Any>()
args.putIfAbsent("PPSceneId",context().sceneCode())
args.putIfAbsent("PPProjectId",context().projectCode())
args.putIfAbsent("Data",fulfills)
Utils.sendDataToDM(context(),"DataReceiverManager","synMaterialFulfillFromPP",args)
var data = (List<Map<String, Any>>) Utils.getDataFromDM(context(),"DataSenderManager","sendIPP_MaterialFulfillToPP",null)
var toAdd = new List<DMFulfill>()
var q = QDMFulfill.qdmfulfill
dh.dslDelete(q).execute()
data.forEach{ map ->
    var obj = new DMFulfill()
    DMFulfill.updateAttributeFromPP(obj,map)
    if (obj.uniqueId == "") {
        return
    }
    toAdd.add(obj)
}
dh.insertOrUpdateBatch(toAdd)
```

## 3. 当前缺少的直接调用方法源码

以下方法在本批代码中只出现了调用，尚未提供方法体：

1. `createInputByOperationBOM`
2. `canUseBomByInputProductId`
3. `BillOfMaterial.querySemiProductIdsByBom`
4. `MaterialSupply.updateAttributeBySemiInventoryNode`
5. `MaterialSupply.updateAttributeByInventoryAvailableNode`
6. `MaterialSupply.updateAttributeByInventoryDetectNode`
7. `setSemiFinishedInventoryDate`
8. `setSemiProductVirtualQuantity`
9. `synPurchaseRequestToMaterialSupply`
10. `MaterialSupply.updateAttributeByIngredientFromDM`
11. `MaterialSupply.updateAttributeByPurchaseOrderln`
12. `MaterialSupply.updateAttributeByProductionPlanFromDM`
13. `MaterialSupply.updateAttributeByPieceStepAndOutput`
14. `MaterialDemand.updateAttributeByPieceStepAndInput`
15. `MaterialSupply.queryNoLocationIdsByProductId`
16. `MaterialSupply.isGuojiChengpingUseBomBanchengping`
17. `MaterialSupply.isGuojiBanchengpingMaterialDemand`
18. `MaterialSupply.queryProcessingTradeHandBooks`
19. `MaterialSupply.filterProcessingTradeHandBookSuppliesInPlace`
20. `MaterialSupply.getGuojiBanchengpingAvailableQty`
21. `MaterialSupply.filterGuojiBanchengpingByProductionMonthInPlace`
22. `MaterialSupply.isInventorySupply`
23. `MaterialDemand.findFirstReplacePriorityByMaterialDemand`
24. `MaterialDemand.sortedByDueDateAndPieceStepId`
25. `MaterialDemand.assignAvailableSuppliesToDemand`
26. `MaterialDemand.queryNextReplacePriorityInSameSetFromInput`
27. `MaterialDemand.assignAvailableSuppliesToDemand1`
28. `MaterialDemand.assignSuppliesToDemandSet`
29. `MaterialDemand.queryNextSetPriorityBySameReplaceFromInput`
30. `MaterialDemand.queryByConsumeSetFromInput`
31. `DMFulfill.updateAttributeFromPP`
32. `updatePieceStepDownStream`

其中继续分析核料分配最需要优先补充的是：

```text
MaterialDemand.assignAvailableSuppliesToDemand
MaterialDemand.assignAvailableSuppliesToDemand1
MaterialDemand.assignSuppliesToDemandSet
MaterialSupply.isGuojiChengpingUseBomBanchengping
MaterialSupply.isGuojiBanchengpingMaterialDemand
MaterialSupply.getGuojiBanchengpingAvailableQty
MaterialSupply.filterGuojiBanchengpingByProductionMonthInPlace
MaterialSupply各类updateAttribute方法
```

## 4. 已注意但未修改的代码点

1. 工序BOM数据按 `queryWorkOrderId` 分组，但使用 `queryOperationBomId` 取值，需要确认两者是否一致。
2. 普通BOM按 `productId+bomVer` 分组，但实际查询BOM只传入 `productId`。
3. 多处分组键直接拼接字段，建议确认不会发生键碰撞；部分替代组键有分隔符，部分没有。
4. `synMaterialSupplyAvailableQuantity` 创建了 `toUpdate`，最后仍执行 `dh.updateBatch(supplies)`。
5. `synInputStepToMaterialDemand` 的删除语句未看到场景过滤条件，需要确认不同场景是否物理隔离。
6. `assignSuppliesToFirstDemands` 中保留了 `pieceId == "A001-10"` 的临时调试代码。
7. 冻结重占统一扣减 `supply.leftQuantity`，是否还需要同步 `leftQuantityVritual`，要结合供应类型确认。
8. 代码中的模型历史字段 `leftQuantityVritual` 拼写应保持不变。
9. 公共供应过滤同时使用 `isGuojiChengpingUseBomBanchengping` 和 `isGuojiBanchengpingMaterialDemand`，两者负责的业务范围需要结合源码确认，避免候选范围与最终过滤条件不一致。
10. 顺位替代料调用公共供应过滤时传入 `firstDemand.demandQuantity` 作为 `leastQty`，会要求单条普通供应达到主需求总量；如果允许多个批次合计满足，可能提前过滤掉合法供应。
11. `assignSuppliesToFirstReplaceDemand1` 中 `totalRemainRatios` 以及循环内的 `totalRemainRatio` 当前没有被使用。
12. `assignSuppliesToFirstReplaceDemand1` 汇总不同需求的供应时未去重，需要确认 `assignAvailableSuppliesToDemand1` 是否自行处理重复供应。
13. 配套组尝试后续优先级时，外层循环没有在 `leftRatio <= Constant.EPS` 后停止，需要确认 `assignSuppliesToDemandSet` 是否会阻止继续分配。
14. `productCompleteQuantityByInv = inputFactor / inputFactor` 在 `inputFactor > 0` 时恒等于1，疑似应使用 `inventoryFulfillQuantity / inputFactor`，需要结合字段业务含义确认。
15. `sendFulfillsToDM` 删除全部 `DMFulfill` 时未看到场景条件，需要确认不同场景是否使用物理隔离表。
